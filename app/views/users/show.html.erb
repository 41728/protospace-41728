<div class="main">
  <div class="inner">
    <div class="user__wrapper">
      <h2 class="page-heading">
       <%= "#{@user.name}さんの情報" %>
      </h2>
      <table class="table">
       <tbody>
         <tr>
           <th class="table__col1">名前</th>
           <td class="table__col2"><%= @user.name %></td>
         </tr>
         <tr>
           <th class="table__col1">プロフィール</th>
           <td class="table__col2"><%= @user.profile %></td>
         </tr>
         <tr>
           <th class="table__col1">所属</th>
           <td class="table__col2"><%= @user.occupation %></td>
         </tr>
         <tr>
           <th class="table__col1">役職</th>
           <td class="table__col2"><%= @user.position %></td>
         </tr>
       </tbody>
     </table>
      <h2 class="page-heading">
        <%= "#{@user.name}のプロトタイプ"%>
      </h2>
      <div class="user__card">
       <% if @prototypes.any? %>
       <% @prototypes.each do |prototype| %>
        <div class="card">
          <% if prototype.image.attached? %>
          <%= link_to image_tag(prototype.image, size: "300x200"), prototype_path(prototype) %>
          <% else %>
          <%= link_to image_tag("no_image.png", size: "300x200"), prototype_path(prototype) %>
          <% end %>
          <h3><%= link_to prototype.title, prototype_path(prototype) %></h3>
          <p><%= prototype.catch_copy %></p>
        </div>
          <% end %>
          <% else %>
          <p>このユーザーはまだプロトタイプを投稿していません。</p>
          <% end %>
     </div>
   </div>
  </div>
</div>